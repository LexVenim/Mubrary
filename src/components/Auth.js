import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Auth extends Component {
  render() {
    return (
      <div>
        Hello in Auth!
      </div>
    );
  }
}

export default Auth;
